require "bundler/gem_tasks"

desc 'Default: A default rake task.'
task :default => :all

desc 'Test formtastic with all supported Rails versions.'
task :all do
  puts "This is the default"
end

task :testerballs do
  puts "you ran my rake"
end
